<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="kordoghli.firas.troc">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
            android:windowSoftInputMode="adjustResize"
            android:name=".data.VolleySingleton"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:usesCleartextTraffic="true"
            android:theme="@style/AppTheme">
        <activity android:name=".UI.session.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".UI.session.CreateAccountActivity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.session.LoginActivity"/>

        <activity android:name=".UI.HomeActivity" android:windowSoftInputMode="stateVisible|adjustNothing"/>

        <activity android:name=".UI.profile.profile.info.ProfileInfoAcrivity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.HomeActivity"/>

        <activity android:name=".UI.profile.mes.offre.MesOffresActivity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.HomeActivity"/>

        <activity android:name=".UI.profile.mes.favoris.MesFavorisActivity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.HomeActivity"/>

        <activity android:name=".UI.MainActivity"/>

        <activity android:name=".UI.ProfileCreateurActivity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.HomeActivity"
                  android:exported="true"/>

        <activity android:name=".UI.DetailsServiceActivity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.HomeActivity"/>

        <activity android:name=".UI.CategorieActivity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.HomeActivity"/>

        <activity android:name=".UI.troquer.TroquerActivity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.HomeActivity"/>

        <activity android:name=".UI.troquer.Etape1Activity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.HomeActivity"/>

        <activity android:name=".UI.troquer.Etape2Activity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.troquer.Etape1Activity"/>

        <activity android:name=".UI.troquer.Etape3Activity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.troquer.Etape2Activity"/>

        <activity android:name=".UI.troquer.Etape4Activity"
                  android:theme="@style/TroquThemeActionBar" android:parentActivityName=".UI.troquer.Etape3Activity"/>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="@string/google_maps_key"/>


    </application>

</manifest>